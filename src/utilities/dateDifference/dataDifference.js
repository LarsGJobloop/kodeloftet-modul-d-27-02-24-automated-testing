/**
 *
 * @param {Date} startDate
 * @param {Date} endDate
 * @returns {[number, number, number, number]} - [dager, timer, minutter, sekunder]
 */
export function dateDifference(startDate, endDate) {
  // Figure out how to return correct values
  // ???
  endDate - startDate;

  const days = 0;
  const hours = 0;
  const minutes = 0;
  const second = 1;

  // I am not saying returning an n-tuple (array with fixed size) is the best option here
  return [days, hours, minutes, second];
}
